<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Goldilocks Atmosphère</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            text-align: center;
            background-color: #f5f5f5;
            margin: 0;
            padding: 20px;
        }
        h1 {
            color: #2c3e50;
        }
        .game-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .slider-container {
            margin: 20px 0;
        }
        .slider {
            width: 100%;
            max-width: 400px;
            margin: 10px auto;
        }
        #earth {
            width: 150px;
            height: 150px;
            margin: 20px auto;
            border-radius: 50%;
            background-color: #1E90FF; /* Bleu pour habitable */
            transition: background-color 0.5s;
        }
        .hot {
            background-color: #FF4500 !important; /* Rouge pour trop chaud */
        }
        .cold {
            background-color: #ADD8E6 !important; /* Bleu clair pour trop froid */
        }
        .controls {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin: 20px 0;
        }
        button {
            padding: 10px 20px;
            font-size: 16px;
            background-color: #3498db;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        button:hover {
            background-color: #2980b9;
        }
        #temperature {
            font-size: 24px;
            font-weight: bold;
            margin: 20px 0;
        }
        .instructions {
            font-style: italic;
            color: #7f8c8d;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="game-container">
        <h1>Goldilocks Atmosphère</h1>
        <p>Ajuster les niveaux de gaz pour garder la Terre habitable !</p>

        <div id="earth"></div>
        <div id="temperature">Température : 20°C</div>

        <div class="slider-container">
            <p>CO₂ : <span id="co2-value">70</span>%</p>
            <input type="range" id="co2" class="slider" min="0" max="100" value="70" step="5">
        </div>

        <div class="slider-contain
